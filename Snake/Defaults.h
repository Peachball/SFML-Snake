#ifndef DEFAULT_HEADER
#define DEFAULT_HEADER
#include <SFML/Graphics.hpp>
const unsigned int SCREENX_SIZE = 800;
const unsigned int SCREENY_SIZE = 600;
const int BOARDX = 10;
const int BOARDY = 10;

namespace COLOR{
	const sf::Color BLACK = sf::Color(0, 0, 0, 255);
	const sf::Color WHITE = sf::Color(255, 255, 255, 255);
}

#endif